CREATE TABLE [dbo].[EmailAddressType] (
    [ID]                   INT          NOT NULL,
    [EmailAddresstypeName] VARCHAR (30) NOT NULL,
    CONSTRAINT [PK_EmailAddressType] PRIMARY KEY CLUSTERED ([ID] ASC)
);

